// Rider Notification Tiers (in km)
export const NOTIFICATION_RADIUS_TIERS = [5, 10, 20];

// Time between notification expansions (in minutes)
export const NOTIFICATION_EXPANSION_MINUTES = 2; // Expand every 2 minutes if no rider found

// Auto-cancel timeout for pending orders (in minutes)
export const AUTO_CANCEL_MINUTES = 5;
